---
description: Produce a UX architecture plan for a UI surface before implementation.
agent: design-engineer
---

Run `/design-arch` for:

$ARGUMENTS

Use the UX Architect workflow. Read root `DESIGN.md`, relevant research briefs if present, and the bundled `modern-design` layout/component references. Save the UX plan under `.design-engineer/briefs/`.
